//about 경로로 들어왔을 때 보여줄 개별 페이지

import React from 'react';
import Menu from 'components/Menu';

const Auth = () => {
    return (
        <div>
            <Menu/>
            Auth
        </div>
    );
};

export default Auth;